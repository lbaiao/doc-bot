Currently, when document upload completes, a new chat is created but no document ID is assigned to it. This is probably an issue on both frontend and backend.

Implement the following flow: document upload completes -> document id is generated -> a new chat is created, and document id is assigned to it.
