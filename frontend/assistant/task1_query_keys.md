Query keys, for Tanstack React Query, are all over the place in the codebase. Swipe the code and centralize all keys in a single file. Those keys should be assigned to constants, and the constants should be referenced across the project instead.

Since this is an important part of the architecture, add it to the documentation. Write a .md file to the documentation dir.
